version: '3'
services:
  app:
    build:
      context: ../
      dockerfile: ./docker/Dockerfile
    container_name: d_python3.10
    image: python:3.10
    volumes:
      - '../:/var/www/html'
    container_name: python
    tty: true
    working_dir: '/var/www/html'
